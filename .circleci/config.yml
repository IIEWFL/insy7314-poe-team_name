version: 2.1

jobs:
  employee-backend-api-tests:
    docker:
      - image: cimg/node:20.10.0
      - image: mongo:6.0
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Install dependencies including express-validator
          command: |
            cd EmployeePortal/Backend
            # Install missing dependency
            npm install express-validator
            npm install
      - run:
          name: Wait for MongoDB
          command: |
            for i in {1..30}; do
              if nc -z localhost 27017; then
                echo "MongoDB is ready!"
                exit 0
              fi
              sleep 2
            done
            echo "MongoDB failed to start"
            exit 1
      - run:
          name: Create test environment
          command: |
            cd EmployeePortal/Backend
            echo "MONGODB_URI=mongodb://localhost:27017/INSY7314" > .env
            echo "PORT=8001" >> .env
            echo "JWT_SECRET=test" >> .env
            echo "USE_HTTPS=false" >> .env
      - run:
          name: Start server and test
          command: |
            cd EmployeePortal/Backend
            
            # Start server in background
            node server.js &
            SERVER_PID=$!
            echo "Server started with PID: $SERVER_PID"
            
            # Wait for server to start
            sleep 8
            
            # Check if server is running
            if ps -p $SERVER_PID > /dev/null; then
              echo "✓ Server process is running"
              
              # Test the endpoints
              echo "=== Testing endpoints ==="
              if curl -s http://localhost:8001/api/employee > /dev/null; then
                echo "✓ API endpoint responding"
              else
                echo "✗ API endpoint not responding"
              fi
              
              # Kill the server
              kill $SERVER_PID
              echo "Server stopped"
            else
              echo "✗ Server process died"
              # Start in foreground to see the actual error
              echo "=== Server output ==="
              node server.js
            fi

  customer-backend-api-tests:
    docker:
      - image: cimg/node:20.10.0
      - image: mongo:6.0
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            cd CustomerPortal/Backend
            # Check if customer backend also needs express-validator
            npm install express-validator
            npm install
      - run:
          name: Wait for MongoDB
          command: |
            for i in {1..30}; do
              if nc -z localhost 27017; then
                echo "MongoDB is ready!"
                exit 0
              fi
              sleep 2
            done
            echo "MongoDB failed to start"
            exit 1
      - run:
          name: Create test environment
          command: |
            cd CustomerPortal/Backend
            echo "MONGODB_URI=mongodb://localhost:27017/INSY7314" > .env
            echo "PORT=8000" >> .env
            echo "JWT_SECRET=test" >> .env
            echo "USE_HTTPS=false" >> .env
      - run:
          name: Start server and test
          command: |
            cd CustomerPortal/Backend
            
            # Start server in background
            node server.js &
            SERVER_PID=$!
            echo "Server started with PID: $SERVER_PID"
            
            # Wait for server to start
            sleep 8
            
            # Check if server is running
            if ps -p $SERVER_PID > /dev/null; then
              echo "✓ Server process is running"
              
              # Test the endpoints
              echo "=== Testing endpoints ==="
              if curl -s http://localhost:8000/api/customer > /dev/null; then
                echo "✓ API endpoint responding"
              else
                echo "✗ API endpoint not responding"
              fi
              
              # Kill the server
              kill $SERVER_PID
              echo "Server stopped"
            else
              echo "✗ Server process died"
              # Start in foreground to see the actual error
              echo "=== Server output ==="
              node server.js
            fi

workflows:
  version: 2
  test-all:
    jobs:
      - employee-backend-api-tests
      - customer-backend-api-tests
